name: barrier_around
description: A Flutter package that creates a barrier around any widget, no matter where it is.
version: 1.0.0
repository: https://github.com/FlutteristDev/barrier_around
issue_tracker: https://github.com/FlutteristDev/barrier_around/issues

environment:
  sdk: ">=2.19.2 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
